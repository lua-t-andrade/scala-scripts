# Binary Search Implementation

## Non-functional

### Usage

This is a binary search implementation. You can change the Vector arr to search on different arrays. Change the input of the `binarySearch()` function to search for a value.

`scala run` will run this project for you!
